export enum ButtonThemeVariantType {
  PRIMARY = "primary",
  SECONDARY = "secondary",
  DANGER = "danger",
  OUTLINE = "outline",
  GHOST = "ghost",
}

export enum ButtonSizeVariantType {
  DEFAULT = "default",
  XS = "xs",
  SM = "sm",
  LG = "lg",
}
