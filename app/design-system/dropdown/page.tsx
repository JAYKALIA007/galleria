import React from "react";
import { DropdownPageWrapper } from "./DropdownPageWrapper";

export const metadata = {
  title: "Dropdown Menu | Design System",
  description: `Display a menu to the user which allows the user to select from a set of
    actions/functions that get triggered by a button.`,
  keywords: ["dropdown-menu", "frontend", "components", "design-system"],
};

const DropdownPage = () => <DropdownPageWrapper />;

export default DropdownPage;
