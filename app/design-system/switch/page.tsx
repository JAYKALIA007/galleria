import { SwitchPageWrapper } from "./SwitchPageWrapper";

export const metadata = {
  title: "Switch | Design System",
  description:
    "A control which allows the user to toggle between checked and unchecked states and opltionally show a label associated with it.",
  keywords: ["frontend", "components", "design-system", "switch"],
};

const ToastPage = () => {
  return <SwitchPageWrapper />;
};

export default ToastPage;
